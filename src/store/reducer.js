export default function(state = {}, action){
    switch (action.type) {
        /*case "SET_TOKEN":
            state.token = action.payload.token;
            return state*/
        default:
          return state
      }
}